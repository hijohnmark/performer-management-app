import React, {useState} from "react"
import { useOutletContext } from "react-router-dom"

const VenueForm = () => {
    return (
        <h1>This is the venue form!</h1>
    )
}

export default VenueForm